# Тестовое задание Артура Громенкова на тему сервиса - анализатора кожных высыпаний
## Что хотелось бы упомянуть перед просмотром решения
1. Скорость - я хотел сделать как можно быстрее, поэтому, вместо логгирования принты, вместо GPT у меня koboldcpp, вместо юниттестов один скрипт который проверяет работу сервиса
2. Код может выглядеть неоднородно, потому что оптимизации и выправлении структуре было уделено недостаточно времени, акцент был сделан на то, чтобы это решение работало.
3. Докер не собирал, но если потребуется, могу и написать докерфайл.
4. Не понял, что подразумевается под "состоянием диалога".

## Ответы по заданию
1. Использована клип модель zer0int/LongCLIP-GmP-ViT-L-14, покороче использовать не стал, захотел количество дескрипторов большим сделать
1.* Для отладки использовалась Qwen3-8B-Q4_K_M, просто потому что она оказалась под рукой
2. Пайплайн устроен просто, приходит запрос, либо с изображением, либо с вопросами, ответами и ближайшим дескриптором (чтобы не перезапускать клип модель). В первом случае, возвращаем вопросы и ближайший дескриптор, во втором - рекомендации. Считаю что так делать нехорошо и на деле надо было два разных запроса делать, но сделал как попросили.
3. Чтобы локально запустить, надо создать среду , установить зависимости из requirements.txt (может там не всё), нужно иметь подключение к кобольду ( и знать на каком порте он запущен) Использовал кобольд, потому что ключей к крупным ЛЛМ у меня под рукой не оказалось, а запросы там не так сильно отличаются. Запустить сервис командой (будучи в среде) python src/app.py и сделать запрос с помощью scripts/test_analyze.py, его использование выглядит примерно так python scripts/test_analyze.py  --port 8022 --image_path  scripts/data/image.png

## Пример запроса/Ответа (Более подробный привер в scripts/test_analyze.py)
import requests

image_path = 'path/to/image.png'

with open(image_path, "rb") as image_file:
    image_data = base64.b64encode(image_file.read()).decode('utf-8')

url = f"http://{host}:{port}/analyze"
payload = {
    "image_data": image_data
}

print(f"📤 Отправка запроса на {url}...")
response = requests.post(url, json=payload, timeout=30)

if response.status_code == 200:
    result = response.json()
    return result

## Образец ответа
{'questions': '1. когда появились высыпания и как быстро они увеличивались?\n2. были ли зуд или другие дискомфортные ощущения при высыпаниях?\n3. имелись ли какие-либо известные контактные аллергии или кожные реакции на препараты?', 'rash_description': 'линейные высыпания по ходу нервных стволов'}



# 📌Исходное задание

1. Постройте простой прототип сервиса, который делает следующее:
1. На вход получает фотографию кожного высыпания (любую — можно из интернета).
2. Прогоняет изображение через любой готовый vision-модельный энкодер (например: CLIP, ViT, EfficientNet).
3. На основе извлечённых признаков передаёт данные в LLM (GPT, Llama, Qwen — любая).
4. LLM должна задать 2–3 уточняющих вопроса, основываясь на визуальных признаках (например: длительность высыпания, зуд, болезненность).
5. После получения ответов — LLM должна выдать 2–3 возможных диагноза (только как предположение!) +
— ключевые отличия между ними,
— какие симптомы уточнить,
— какие методы диагностики нужны (общие, безопасные).

2. Требования к архитектуре
• Backend на FastAPI или Flask.
• Один endpoint /analyze принимающий:
• фото,
• ответы на вопросы (если есть),
• состояние диалога.
• Результат: JSON с
• уточняющими вопросами или
• финальным ответом (дифдиагноз + рекомендации).

3. Что предоставить по итогу

Кандидат должен прислать:
1. Ссылку на GitHub с кодом.
2. README, где кратко описано:
• какие модели использованы,
• как устроен pipeline,
• как запустить локально.
3. Пример запроса/ответа (JSON).
4. (Опционально) — короткое видео-демо 1–2 минуты.